import cv2
import numpy as np
import matplotlib.pyplot as plt

img = cv2.imread('name_of_your_image.extension', cv2.IMREAD_GRAYSCALE)

cv2.imshow('image', img)
cv2.waitKey(0)
cv2.destroyAllWindows() #it destroys the window on pressing any key
                        #You can skip it if you do not want it

    #Plotting using matplotlib
##plt.imshow(img, cmap='gray', interpolation = 'bicubic')
##plt.plot([200,200],[800,0], 'c', linewidth=5)
##plt.show()
